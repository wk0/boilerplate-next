import { ethers } from 'ethers'
export const Test = ({}) => {
  const a = ethers.utils.parseUnits('2', 'ether')
  const b = ethers.utils.parseUnits('2', 'ether')
  // console.log('a', a)
  // console.log(a.mul(b))
  return <div></div>
}
